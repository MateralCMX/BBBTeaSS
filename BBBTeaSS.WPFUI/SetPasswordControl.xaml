<UserControl x:Class="BBBTeaSS.WPFUI.SetPasswordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BBBTeaSS.WPFUI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="1" Margin="10,0">新密码</Label>
        <PasswordBox x:Name="TextNewPassword1" Grid.Row="2" Margin="10,0"></PasswordBox>
        <Label Grid.Row="4" Margin="10,0">确认新密码</Label>
        <PasswordBox x:Name="TextNewPassword2" Grid.Row="5" Margin="10,0"></PasswordBox>
        <Button x:Name="BtnOK" Grid.Row="7" Margin="10,0" Click="BtnOK_Click">确认修改</Button>
    </Grid>
</UserControl>
